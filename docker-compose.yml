
services:
  api:
    build: .
    container_name: passos_magicos_api
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models
      - ./logs:/app/logs
    environment:
      - ENVIRONMENT=production
    restart: unless-stopped